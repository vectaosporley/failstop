# Shows the "Sponsor" button at the top of the repo.
# Cafecito is an Argentine donation platform; GitHub has no native key for it,
# so it goes in `custom` as a plain URL. Add more lines here later if you want
# (github: for GitHub Sponsors, ko_fi:, etc.).
custom: ["https://cafecito.app/vecta"]
